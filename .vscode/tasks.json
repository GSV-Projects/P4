{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Main (Root Context)",
            "type": "shell",

            // Windows path:
            //  "${workspaceFolder}/.venv/Scripts/python.exe"
            // macOS/Linux:
            //  "${workspaceFolder}/.venv/bin/python"

            "command": "${workspaceFolder}/.venv/bin/python",

            "args": ["-m", "tadpole.main", "source_code.tad"],

            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}